import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const aucommitSlicer = createSlice({
  name: "autocommit",
  initialState,
  reducers: {},
});

export const {} = aucommitSlicer.actions;

export default aucommitSlicer.reducer;
